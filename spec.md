你现在是一个资深的前端创意开发工程师、交互设计师和产品架构师，请为我完整设计并实现一个高完成度、可直接运行和持续迭代的 Web 项目，项目名称为“交互式魔法机械图鉴工坊（Arcane Machine Codex Workshop）”，这是一个以 SVG 为核心渲染技术的“可编辑 + 可动画 + 可组合”的魔法机械/符文装置生成器，用户进入后可以像拼装电路图、炼金装置或魔法仪器一样，在画布中拖拽、摆放、旋转、缩放、连接和组合各种 SVG 模块，构造出一台具有独特视觉结构和属性设定的“魔法机器”，模块类型至少包括核心炉心、能量管道、齿轮机构、符文节点、防护外壳、触发开关、输出法阵，并允许后续扩展更多部件类别；整体项目必须围绕 SVG 的优势展开设计，充分利用矢量拼装、局部动画、描边流动、渐变发光、路径动画、图层分组、滤镜、mask、pattern、symbol、defs、clipPath、stroke-dasharray、stroke-dashoffset、transform-origin、animateMotion 或基于 JS/动画库的路径驱动动画等能力，让每一个零件既可以独立编辑，也可以组合后形成一个统一完整、风格强烈、可动态激活的魔法机械系统；请将项目设计为一个兼具“创作工具 + 动画展示 + 图鉴收集 + 随机生成 + 导出分享”的完整产品，而不是一个简单的单页面演示，要求具备明确的信息架构、页面结构、组件结构、状态管理方案和可扩展的数据模型，并生成尽可能专业的项目方案与代码实现；产品界面风格要求统一且高级，整体视觉定位为“魔法炼金术 × 蒸汽机械 × 赛博符文”的混合美术方向，主界面使用深色背景和高对比发光元素，局部采用金属边框、符文圆环、几何网格、流体能量管线、机械齿轮和法阵纹样，界面应兼具神秘感、科技感和收藏图鉴的仪式感，避免廉价卡通感，要求在低美术资源条件下主要通过 SVG 线条、形状、渐变、阴影、滤镜和重复几何图形做出丰富效果；功能层面必须包含一个高可用的“机械装置编辑器”，用户可以从左侧或顶部的模块资源面板中选择模块拖入中央工作台，支持吸附、对齐、层级调整、复制、删除、旋转、缩放、镜像、颜色替换、样式切换、插槽连接和组合成组，画布支持缩放、平移、网格辅助、选中态高亮、框选、撤销重做、快速清空和自动布局建议；必须包含一个“能量连接系统”，用户拖拽连接点时可在模块之间自动生成能量路径，路径采用 SVG path 渲染并带有动态的流动光点、电流描边、颜色状态变化、过载闪烁、脉冲传播和连接成功反馈，不同模块之间的连接会影响机器的属性和激活效果，连接逻辑应具备基础规则系统，例如输入/输出端口、元素属性、稳定性、回路完整度和冲突提示；必须包含一个“激活预览系统”，当用户点击“启动机器”后，整台装置进入动态状态，不同模块按层级和依赖顺序触发动画，表现至少包括齿轮转动、符文脉冲、能量流动、核心闪烁、法阵亮起、外壳抖动、故障闪烁、超载爆发和最终一次大的启动/喷发/共鸣效果，动画要有节奏感，不是简单同时播放，建议有 idle、charging、active、overload、failure、shutdown 等状态机，并允许用户切换正常模式与故障模式；必须包含“机器属性生成系统”，根据用户的模块组合、连接方式、配色、能量属性和结构复杂度，自动生成机器名称、稀有度、属性标签、故障率、输出类型、稳定性、能耗、核心派系、描述文案和图鉴编号，例如“虚空回响增幅器”“熔星相位解构炉”“低稳定 / 高爆发 / 雷属性 / 深渊派系”，命名和描述应有规则感和世界观味道，可以先用本地规则生成器实现，再预留接入 AI 文本生成接口的能力；必须包含“图鉴系统”，用户创建完成的每一台机器都可以保存为图鉴条目，图鉴页应包含标题、缩略图、机器主视图、模块清单、属性说明、稀有度徽章、状态标签、创建时间、作者名（可选）、激活动画预览入口、背景设定文字和分享导出按钮，图鉴系统支持列表视图、详情视图、筛选排序、按属性或派系统计、收藏、重命名和删除；必须包含“随机生成模式”，提供一键随机锻造功能，系统基于模块库、布局规则、连接规则和风格主题，自动生成一台结构合理、视觉完整、可激活的魔法机器，并同步生成属性与名称，随机生成结果不能太乱，要具有审美控制和可编辑性；导出能力方面，必须支持导出 SVG、PNG，并尽量支持导出图鉴卡海报形式，导出结果要保留主要视觉层级，图鉴卡页面可包含标题、主图、属性面板、装饰边框、派系印章和说明文字，适合分享到社交媒体或作品集中；技术方案建议优先使用 React + TypeScript + Vite 作为基础框架，状态管理可用 Zustand 或 Redux Toolkit，SVG 编辑交互可结合原生 SVG + d3-path / svg.js / Framer Motion / GSAP 等实现，布局与 UI 使用 Tailwind CSS 或自定义设计系统，数据模型应清晰拆分为模块定义、实例数据、连接数据、动画状态、图鉴数据和主题配置，模块库应采用配置驱动设计，例如每类模块由 metadata + SVG 模板 + 连接点 + 属性影响规则 + 可调参数组成，后续新增模块无需大改架构；请确保系统具备良好的工程性和可扩展性，至少明确目录结构、核心组件职责、状态流、数据结构、事件机制、命令模式（用于撤销重做）、主题系统、动画系统和导出流程，并为后续扩展出“AI 命名描述助手”“社区分享广场”“图鉴交易/交换”“挑战任务模式”“符文配方解锁系统”“派系科技树”等玩法预留接口；交互层面要求尽量细腻，例如拖入模块时有吸附和落点反馈，连接时路径动态预览，选中模块时显示可编辑锚点和参数面板，激活时镜头或画布有轻微聚焦和震动感，故障状态会有红色闪烁、断续电流和异常噪点，图鉴保存时有仪式化收纳动画；请在最终输出中给出完整且高标准的项目定义，包括产品定位、目标用户、核心卖点、页面与功能模块规划、技术架构设计、组件与数据结构设计、SVG 元件设计规范、动画表现方案、命名与属性生成规则、图鉴系统设计、随机生成逻辑、导出逻辑、可扩展方向，以及一版可直接开始开发的详细实现方案，如果需要直接产出代码，请优先实现一个可运行的 MVP，MVP 也必须包含模块拖拽编辑、至少 6 类 SVG 模块、路径连接、基础激活动画、属性生成、图鉴保存和 SVG/PNG 导出，不要做成只有静态展示的玩具页面，而要做成结构完整、交互丰富、视觉统一、具备作品级完成度的创意工具型项目。